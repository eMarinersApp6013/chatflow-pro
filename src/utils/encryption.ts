// Placeholder — Phase 6 will add message content encryption for private notes.
// For now just re-exports the raw value.

export function encrypt(value: string): string {
  return value;
}

export function decrypt(value: string): string {
  return value;
}
